#include "main.h"

/**
 * _abs - print the absolute balue
 * @n: The integer to be computed.
 * Return: The absolute value of the integer.
 */
int _abs(int n)
{
	if (n >= 0)
		return (n);
	else
		return (-n);
}

